anthropic==0.105.2
langgraph==1.2.5
# LangGraph SQLite checkpointer (Sprint 1 foundation; lazy-imported in graph/checkpoint.py)
langgraph-checkpoint-sqlite==3.1.0
pydantic==2.13.4
apify-client==1.7.0
python-docx==1.2.0
openpyxl==3.1.5
python-dotenv==1.2.2
requests==2.34.2
langdetect==1.0.9
# Phase 3 — semantic skill matching
# Install CPU-only torch (~800MB) to avoid downloading the CUDA build (~2GB):
#   pip install sentence-transformers torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==5.5.1
# Phase 8 — productionization (API layer)
fastapi==0.137.1
uvicorn[standard]==0.49.0
